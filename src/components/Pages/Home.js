import React from "react";
import { data } from "./../../data/data";
import Card from "./../../Ui/Card/Card";

const Home = () => {
  return (
    <div>
      <Card>home</Card>
      {/* {data.map((i) => (
        <Card>{i}</Card>
      ))} */}
    </div>
  );
};

export default Home;
